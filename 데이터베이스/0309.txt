무결성?

24PG 물리적 접근? 논리적 접근?

계층 네트워크 DB의 문제 = Link로 연결되어 있기 때문에 구조적인 문제가 있다 = 관계 DBMS의 등장

정의어 = 스키마를 정의, 조작어 = 데이터 (레코드)를 조작, 수정, 검색, 제어어 = 트랜잭션 명시, 보안을 위해 권한을 부여, 취소

-

DB 관리자 = 관리자 도구를 통해 DB를 관리한다
설계자 = DB의 스키마나 구조를 만들어내는 사람
프로그래머 = 질의 도구를 통해서 원해는 데이터를 찾아내 어플 등을 만든다
오퍼레이터 = 서버 관리자, 물리적인 서버들에 대한 전산실 관리자

기작성 = 어떤 작업에 대한 질의를 하나로 만들어놓음 - 단순히 버튼을 클릭한다거나 하는 작업만으로 여러 과정을 수행함

정규화 = DB의 일관성을 유지하기 위한 작업

52PG 뷰 사용자들에게 제공할 목적으로 원래의 테이블에서 특정 정보만을 추출해서 만드는 가상 테이블

추상화 되었다 = 외부 단계의 뷰를 가지고 있는 사용자들은 하위 단계들이 어떻게 이루어져 있는지는 알 필요가 없다
사상 = 두 단계를 이어주는 mapping 과정

55PG *

58PG 전체 지하철 노선도 = 개념 스키마, 필요한 지하철 노선도 = 외부

예) 사용자가 필요한 정보는 개념 스키마에 저장되어 있는 레코드의 내용과 매핑되어 있지 않다 따라서 외부 / 개념 사상이 필요한 정보들을 가져오기 위해 매핑을 해주는 것

독립성 = 아랫 (내부) 단계를 변화시킨다고 해도 윗 (외부) 단계에서는 변화하면 안된다

분산 DB = 네트워크로 연결되어 있기 때문에 사용자 입장에서 하나의 DB처럼 움직인다 하나의 DB에 질의를 날리면 네트워크를 통해서 다른 DB에서도 정보를 받아와서 데이터를 반환한다